{
  "name": "Browser_Prompt",
  "description": "Browser_Prompt triggers a prompt dialog in the user's browser that asks for input. The user's response will be sent back to the AI automatically as a new message.",
  "parameters": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "The message/question to display in the prompt dialog"
      }
    },
    "required": [
      "message"
    ]
  }
}
